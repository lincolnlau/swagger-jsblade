{@include file://./../../header-template.juicer, _}
(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD 格式
        define([
            'axios'
        ], factory);
    } else if (typeof exports === 'object') {
        // CommonJs 格式
        module.exports = factory(require('axios'));
    } else {
        // 挂载到浏览器 window 下
        root.$${config.apiName} = factory(root.axios);
    }
}(this, function (axios) {
// 子模板替换位置
}));
