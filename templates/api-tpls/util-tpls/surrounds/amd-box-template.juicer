{@include file://./../common/header-template.juicer, _}
define([{@if config.importName}
    '$${config.importName}'
{@/if}], function ({@if config.exportName}$${config.exportName}{@/if}) {
// 子模板替换位置
});
