module.exports = {
  mockDir: '/mock',
  basePath: '/',
  path: {
    {@each swagger.paths as path,index}
        {@each path as req,key}
    '$${index}': 1,
        {@/each}
    {@/each}
  },
  proxy: {}
}