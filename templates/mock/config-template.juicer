module.exports = {
  mockDir: 'mock',
  basePath: '/',
  path: {
    {@each swagger.paths as path,index}
        {@each path as req,key}
    '$${index}': 0,
        {@/each}
    {@/each}
  },
  proxy: {}
}